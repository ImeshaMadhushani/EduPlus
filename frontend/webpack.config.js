/* const path = require('path');

module.exports = {
    module: {
        rules: [
            {
                test: /\.js$/,
                enforce: 'pre',
                use: ['source-map-loader'],
                exclude: [
                    /node_modules\/lucide-react/,
                    /node_modules\\lucide-react/  // for Windows paths
                ]
            }
        ]
    },
    ignoreWarnings: [/Failed to parse source map/]
}; */